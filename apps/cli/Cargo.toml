[package]
name = "ai-task-scheduler-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
scheduler-core = { path = "../../crates/scheduler-core" }
chrono = "0.4"
clap = { version = "4", features = ["derive"] }
serde_json = "1"
